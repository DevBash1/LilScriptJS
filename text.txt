<br>

<div id="board">
Using LilScriptJS
</div>
<div id="write">
To use <code>LilScriptJS</code>, we have to first set the script type to <code>text/lilscript</code> in our script tag.
Then write our code into the script tag or use the src attribute in our <code>JavaScript</code> code.<br>
</div>
<textarea id="use">

</textarea>
<center><button type="button" id="run15" class="btn btn-dark run">Run</button></center>
<script>
  let use = document.getElementById("use");
  var editor15 = CodeMirror.fromTextArea(use, {
    lineNumbers: true,
	matchBrackets: true,
    mode: 'javascript',
  });
</script>
<script type="text/lilscript">
  let run = @("#run15");
  run.on("click",function(){
	  try{
		  eval(ls.run(editor15.getValue()));
	  }catch(e){
		  box e;
	  }
  })
</script>